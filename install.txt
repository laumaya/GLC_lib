GLC_lib INSTALLATION

QT4 must be installed

To compile the library on windows or linux
	cd GLC_lib
	qmake
	make all

	For windows copy GLC_lib.dll on window\sywtem32
	For linux as root copy the librarys
	cp -p GLC_lib*so* /usr/lib
	ldconfig

